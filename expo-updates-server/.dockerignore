node_modules
.next
.git
.gitignore
# env files must not enter the image — .env.local's HOSTNAME would hijack Next's
# socket bind address. Runtime config is injected via docker-compose instead.
.env
.env.local
.env.*
# updates payload + private key are mounted as volumes at runtime, not built in
updates
code-signing-keys/private-key.pem
