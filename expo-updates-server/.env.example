# Copy to .env before `docker compose up`. Sets the hostname Traefik routes and
# the base URL the manifest uses for asset links. One value flips test ↔ prod.
#
#   Test:  OTA_HOST=ota.smitherytech.com
#   Prod:  OTA_HOST=ota.fitarch.my
OTA_HOST=ota.smitherytech.com
